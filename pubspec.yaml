name: supa_l10n_manager
description: CLI utilities for localization merge, extract, reorder, and code generation.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.7.0
  logging: ^1.3.0
  path: ^1.9.1
